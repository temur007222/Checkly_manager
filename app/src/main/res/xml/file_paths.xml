<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow access to cache directory -->
    <cache-path
        name="camera_images"
        path="." />

    <!-- Allow access to external files (Pictures dir) if needed -->
    <external-files-path
        name="external_camera_images"
        path="Pictures" />
</paths>
